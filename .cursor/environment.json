{
  "name": "Mapa de Calor",
  "install": "cd backend && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt",
  "terminals": [
    {
      "name": "api",
      "command": "cd backend && .venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000"
    }
  ],
  "ports": [
    {
      "port": 8000,
      "name": "Mapa de Calor API"
    }
  ]
}
